# Copy to .env and fill in, or export directly in your shell.
# Without a key the pipeline still runs end to end on deterministic fallbacks,
# and every output is labelled "fallback-lexicon" / "fallback-template".
GEMINI_API_KEY=your-key-here
